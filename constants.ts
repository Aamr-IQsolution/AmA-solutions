/**
 * Public barrel for site content and i18n.
 * Source of truth lives under `content/` — import from here for stable paths.
 */
export {
  INITIAL_CONFIG,
  SITE_URL,
  PAGE_META,
  UI_TEXTS,
  type PageMetaKey,
} from './content';
